{
  "name": "@pascca/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "echo '@pascca/types has no dev server' && exit 0",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@pascca/config": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
