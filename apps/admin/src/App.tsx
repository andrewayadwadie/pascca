// Empty shell — no dashboard screen ships in this feature (Article 11: build the phase in
// front of you). apps/admin deliberately has no database/Prisma/server dependency; Article 14's
// "no DB access" is enforced by absence, not by review.
export function App() {
  return null;
}
