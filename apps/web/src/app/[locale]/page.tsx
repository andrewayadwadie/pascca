// Empty shell — no page content ships in this feature (AR-003, Article 11: build the phase in
// front of you). The first content feature replaces this.
export default function HomePage() {
  return null;
}
