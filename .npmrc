# engine-strict here does NOT work with pnpm 11 (verified during implementation of
# 001-monorepo-scaffold — pnpm silently downgrades it to a warning). The actual Node-version
# gate is pnpm-workspace.yaml's `engineStrict: true`. Kept for npm-compatible tooling only.
engine-strict=true
